PORT=4000
CLIENT_URL=http://localhost:5173
APP_URL=http://localhost:5173
JWT_SECRET=change-this-secret-in-production
DB_HOST=localhost
DB_PORT=3306
DB_NAME=prachub
DB_USER=root
DB_PASSWORD=""
NODE_ENV=development

# Email — En desarrollo (NODE_ENV=development) se usa Ethereal automáticamente.
# El link de vista previa del correo se imprime en la consola del servidor.
# Las vars SMTP solo son necesarias en producción (NODE_ENV=production).
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=dannie.hoeger66@ethereal.email
SMTP_PASS=kzbTTcV6GsvaW5zWx6
SMTP_FROM="PracHub <dannie.hoeger66@ethereal.email>"

GEMINI_API_KEY=tu-api-key

# http://localhost:5173/login/admin
# Primera clave en el .env del frontend (client)
# Segunda clave de acceso .env del backend (server)

# Admin security — Secret key required for admin login (double verification)
ADMIN_ACCESS_SECRET=MiClaveSuperSecreta123!
ADMIN_ALERT_EMAIL=admin@prachub.com
ADMIN_SEED_EMAIL=admin@prachub.com
ADMIN_SEED_PASSWORD=PasswordSegura123!